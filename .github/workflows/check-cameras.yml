name: Check BEACH CAM cameras

on:
  schedule:
    - cron: '0 6 */2 * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check camera status
        run: python tools/check_cameras.py
