name: Build BEACH CAM CAMPER APK (legacy sideload)

on:
  workflow_dispatch:

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - name: Sideload APK disabled by default
        run: |
          echo 'Use the Google Play AAB workflow for family distribution.'
          echo 'This legacy APK path remains manual only to avoid accidental Play Protect sideload builds.'
